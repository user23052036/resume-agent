{
  "version": 2,
  "builds": [
    { "src": "frontend/package.json", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/frontend/$1" }
  ],
  "functions": {
    "frontend/app/api/**/*.ts": {
      "runtime": "@vercel/node"
    }
  }
}
